<Window x:Class="TerminalLauncher.Views.AppDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        WindowStyle="None" AllowsTransparency="True" Background="Transparent"
        ShowInTaskbar="False" ResizeMode="NoResize" SizeToContent="WidthAndHeight"
        WindowStartupLocation="CenterOwner"
        FontFamily="{DynamicResource Font.Ui}" FontSize="13"
        TextOptions.TextFormattingMode="Ideal">
    <Border Background="{DynamicResource Brush.Surface}" BorderBrush="{DynamicResource Brush.Border}"
            BorderThickness="1" CornerRadius="12" Padding="24" Margin="14">
        <Border.Effect>
            <DropShadowEffect Color="#000000" BlurRadius="28" ShadowDepth="3" Opacity="0.5"/>
        </Border.Effect>
        <StackPanel MinWidth="300" MaxWidth="460">
            <TextBlock x:Name="TitleText" FontSize="16" FontWeight="SemiBold"
                       Foreground="{DynamicResource Brush.Text}" Margin="0,0,0,10"/>
            <ScrollViewer MaxHeight="360" VerticalScrollBarVisibility="Auto"
                          HorizontalScrollBarVisibility="Disabled">
                <ScrollViewer.Resources>
                    <Style TargetType="ScrollBar" BasedOn="{StaticResource ThemedScrollBar}"/>
                </ScrollViewer.Resources>
                <TextBlock x:Name="MessageText" TextWrapping="Wrap" LineHeight="21"
                           Foreground="{DynamicResource Brush.TextMuted}"/>
            </ScrollViewer>
            <WrapPanel x:Name="ButtonPanel" HorizontalAlignment="Left" Margin="0,20,0,0"/>
        </StackPanel>
    </Border>
</Window>
