<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <!--
        نظام تصميم «فلوينت» خاصّ بشاشة الإعدادات فقط — مستوحًى من لغة Windows 11 Settings البصريّة
        (الأسطح الطبقيّة، الاستدارة الناعمة، الحدود الشعريّة، مفاتيح التبديل، مؤشّر التحديد الرفيع)
        دون نسخ محتواها أو بنيتها. الألوان كلّها DynamicResource على فراشي الثيم الحاليّة
        (Brush.Bg/Surface/Surface2/SurfaceHover/Accent/Text/TextMuted/Hairline...) — فتتبع أيّاً
        من الـ27 ثيماً المتاحة تلقائياً، ولا تُدمَج هذه القائمة إلا داخل SettingsPanel.Resources
        كي لا تُغيّر شكل أيّ عنصر خارج شاشة الإعدادات (الأنماط المشتركة في Theme.xaml تبقى كما هي).

        المفاتيح المُصدَّرة:
          Fluent.ToggleSwitch  — بديل AppCheckBox لإعدادات التشغيل/الإيقاف (مفتاح منزلق حقيقيّ).
          Fluent.NavItem       — بديل SettingsNavButton لقائمة الفئات (مؤشّر رفيع بدل حدّ كامل).
          Fluent.ComboBox      — قائمة منسدلة بظلّ ناعم خلف اللوحة المنبثقة.
    -->

    <!-- ========== مفتاح تبديل (Toggle Switch) ==========
         مسار 38×20 بلونين متراكبين (إيقاف/تشغيل) يتلاشى أحدهما إلى الآخر، ومقبض دائريّ ينزلق فوقهما
         — بدل التلوين المباشر لبرشٍ واحدة، لأنّ WPF لا يحرّك ColorAnimation على DynamicResource
         بسلاسة؛ التلاشي بالعتامة بين طبقتين هو نفس أسلوب انتقالات خلفيّة النافذة المعتمد في التطبيق. -->
    <Style x:Key="Fluent.ToggleSwitch" TargetType="CheckBox">
        <Setter Property="Foreground" Value="{DynamicResource Brush.Text}"/>
        <Setter Property="FontSize" Value="{DynamicResource Size.Small}"/>
        <Setter Property="Cursor" Value="Hand"/>
        <Setter Property="Padding" Value="10,0,0,0"/>
        <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="CheckBox">
                    <StackPanel Orientation="Horizontal" Background="Transparent">
                        <Grid Width="38" Height="20" VerticalAlignment="Center">
                            <Border x:Name="trackOff" CornerRadius="10"
                                    Background="{DynamicResource Brush.Surface2}"
                                    BorderBrush="{DynamicResource Brush.Border}" BorderThickness="1"/>
                            <Border x:Name="trackOn" CornerRadius="10" Opacity="0"
                                    Background="{DynamicResource Brush.Accent}"/>
                            <Border x:Name="focusRing" CornerRadius="12" Margin="-2"
                                    BorderBrush="{DynamicResource Brush.Accent}" BorderThickness="2" Opacity="0"/>
                            <Ellipse x:Name="thumb" Width="14" Height="14" Fill="{DynamicResource Brush.OnAccent}"
                                     HorizontalAlignment="Left" VerticalAlignment="Center" Margin="3,0,0,0">
                                <Ellipse.RenderTransform>
                                    <TranslateTransform x:Name="thumbShift" X="0"/>
                                </Ellipse.RenderTransform>
                            </Ellipse>
                        </Grid>
                        <ContentPresenter VerticalAlignment="Center" Margin="{TemplateBinding Padding}"/>
                    </StackPanel>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsChecked" Value="True">
                            <Trigger.EnterActions>
                                <BeginStoryboard>
                                    <Storyboard>
                                        <DoubleAnimation Storyboard.TargetName="trackOn" Storyboard.TargetProperty="Opacity"
                                                          To="1" Duration="0:0:0.15"/>
                                        <DoubleAnimation Storyboard.TargetName="thumbShift" Storyboard.TargetProperty="X"
                                                          To="18" Duration="0:0:0.15">
                                            <DoubleAnimation.EasingFunction><CubicEase EasingMode="EaseOut"/></DoubleAnimation.EasingFunction>
                                        </DoubleAnimation>
                                    </Storyboard>
                                </BeginStoryboard>
                            </Trigger.EnterActions>
                            <Trigger.ExitActions>
                                <BeginStoryboard>
                                    <Storyboard>
                                        <DoubleAnimation Storyboard.TargetName="trackOn" Storyboard.TargetProperty="Opacity"
                                                          To="0" Duration="0:0:0.15"/>
                                        <DoubleAnimation Storyboard.TargetName="thumbShift" Storyboard.TargetProperty="X"
                                                          To="0" Duration="0:0:0.15">
                                            <DoubleAnimation.EasingFunction><CubicEase EasingMode="EaseOut"/></DoubleAnimation.EasingFunction>
                                        </DoubleAnimation>
                                    </Storyboard>
                                </BeginStoryboard>
                            </Trigger.ExitActions>
                        </Trigger>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="trackOff" Property="BorderBrush" Value="{DynamicResource Brush.Accent}"/>
                        </Trigger>
                        <Trigger Property="IsKeyboardFocused" Value="True">
                            <Setter TargetName="focusRing" Property="Opacity" Value="1"/>
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter Property="Opacity" Value="0.5"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- ========== عنصر تنقّل الفئات ==========
         بدل حدّ اللكنة الكامل حول العنصر المحدَّد (كان يسحب العين): سطح أفتح بخفوت + مؤشّر رفيع
         3px عند حافّة البداية — نفس مبدأ Windows 11 Settings: اللكنة تُشير لا تُهيمن. -->
    <Style x:Key="Fluent.NavItem" TargetType="RadioButton">
        <Setter Property="Foreground" Value="{DynamicResource Brush.Text}"/>
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="Cursor" Value="Hand"/>
        <Setter Property="FontSize" Value="{DynamicResource Size.Ui}"/>
        <Setter Property="Margin" Value="0,0,0,4"/>
        <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
        <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="RadioButton">
                    <Border x:Name="bd" CornerRadius="{DynamicResource Radius.Control}" Padding="14,11"
                            Background="{TemplateBinding Background}">
                        <Grid>
                            <Border x:Name="indicator" Width="3" CornerRadius="1.5"
                                    HorizontalAlignment="Left" VerticalAlignment="Stretch" Margin="-14,2,0,2"
                                    Background="{DynamicResource Brush.Accent}" Opacity="0"/>
                            <ContentPresenter VerticalAlignment="Center"/>
                        </Grid>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="bd" Property="Background" Value="{DynamicResource Brush.RowHover}"/>
                        </Trigger>
                        <Trigger Property="IsChecked" Value="True">
                            <Setter TargetName="bd" Property="Background" Value="{DynamicResource Brush.SurfaceHover}"/>
                            <Setter TargetName="indicator" Property="Opacity" Value="1"/>
                            <Setter Property="Foreground" Value="{DynamicResource Brush.Accent}"/>
                            <Setter Property="FontWeight" Value="SemiBold"/>
                        </Trigger>
                        <Trigger Property="IsKeyboardFocused" Value="True">
                            <Setter TargetName="bd" Property="Background" Value="{DynamicResource Brush.RowHover}"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- ========== قائمة منسدلة ==========
         نسخة عن نمط ComboBox المشترك زائد ظلّ ناعم خلف اللوحة المنبثقة (القسم ١٩/٢٤ من المواصفة:
         Popup يجب أن يحمل عمقاً أوضح من البطاقة). لا تُستبدَل ComboBox المشتركة نفسها كي لا يتغيّر
         شكل القوائم المنسدلة في بقيّة التطبيق. -->
    <Style x:Key="Fluent.ComboBox" TargetType="ComboBox">
        <Setter Property="Foreground" Value="{DynamicResource Brush.Text}"/>
        <Setter Property="Background" Value="{DynamicResource Brush.Surface2}"/>
        <Setter Property="BorderBrush" Value="{DynamicResource Brush.Border}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="Height" Value="34"/>
        <Setter Property="FontSize" Value="{DynamicResource Size.Small}"/>
        <Setter Property="Cursor" Value="Hand"/>
        <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ComboBox">
                    <Grid>
                        <ToggleButton Style="{StaticResource ComboToggle}"
                                      IsChecked="{Binding IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}"/>
                        <Border x:Name="bd" CornerRadius="{DynamicResource Radius.Control}"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                Padding="12,0,10,0" IsHitTestVisible="False">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <ContentPresenter Content="{TemplateBinding SelectionBoxItem}"
                                                  ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}"
                                                  VerticalAlignment="Center"/>
                                <TextBlock Grid.Column="1" Text="&#xE70D;" FontFamily="Segoe MDL2 Assets" FontSize="8"
                                           VerticalAlignment="Center" Margin="8,0,0,0"
                                           Foreground="{DynamicResource Brush.TextMuted}"/>
                            </Grid>
                        </Border>
                        <Popup IsOpen="{TemplateBinding IsDropDownOpen}" Placement="Bottom"
                               AllowsTransparency="True" Focusable="False" PopupAnimation="Slide">
                            <Border CornerRadius="{DynamicResource Radius.Control}" Margin="0,6,0,0" Padding="4"
                                    Background="{DynamicResource Brush.GlassStrong}"
                                    BorderBrush="{DynamicResource Brush.Hairline}" BorderThickness="1"
                                    MinWidth="{Binding ActualWidth, RelativeSource={RelativeSource TemplatedParent}}">
                                <Border.Effect>
                                    <DropShadowEffect Color="#000000" BlurRadius="24" ShadowDepth="4" Opacity="0.35"/>
                                </Border.Effect>
                                <ScrollViewer MaxHeight="{TemplateBinding MaxDropDownHeight}"
                                              VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
                                    <ScrollViewer.Resources>
                                        <Style TargetType="ScrollBar" BasedOn="{StaticResource ComboScroll}"/>
                                    </ScrollViewer.Resources>
                                    <ItemsPresenter/>
                                </ScrollViewer>
                            </Border>
                        </Popup>
                    </Grid>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="bd" Property="BorderBrush" Value="{DynamicResource Brush.Accent}"/>
                        </Trigger>
                        <Trigger Property="IsKeyboardFocusWithin" Value="True">
                            <Setter TargetName="bd" Property="BorderBrush" Value="{DynamicResource Brush.Accent}"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

</ResourceDictionary>
