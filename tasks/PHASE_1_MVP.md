# Phase 1 — MVP (تيرمنال قابل للاستخدام اليومي)
> **شرط البدء:** اجتياز Phase 0 Exit Test كاملاً.
> **الهدف النهائي:** استبدال Windows Terminal بالاستخدام اليومي.

---

## T-101: Multi-Shell Profiles
**الحالة:** ✅ مكتمل | **الأولوية:** P0 | **التقدير:** 4 أيام | **يعتمد على:** T-002

### Subtasks
- [x] T-101.1: موديل `Models/ShellProfile.cs`: Id, Name, Icon, ExePath/Args, WorkingDirectory, EnvironmentVariables, TabColor
- [x] T-101.2: `Services/ShellDetector.cs`: CMD, PowerShell 5, pwsh (PATH+مسار معروف), Git Bash (مسارات+اشتقاق من git.exe), WSL (`wsl.exe -l -q`، كشف UTF-16/8 استدلالي)
- [x] T-101.3: تخزين `Services/ProfileStore.cs` → profiles.json + بروفايل افتراضي قابل للتحديد
- [~] T-101.4: dropdown بزرّ التاب الجديد ✅ + إدارة (إضافة/تعديل/حذف/تعيين افتراضي) ✅؛ **الترتيب (drag) والأيقونات/TabColor مؤجَّلة (UI)**
- [x] T-101.5: تمرير WorkingDirectory و EnvVars للجلسة (`StartSession` + `ApplyProfileEnvironment`)

### معايير القبول
- [x] كل الـ shells المثبتة تنكتشف تلقائياً (تدرّج بأمان: لا pwsh/WSL على جهاز التطوير ⇒ لا يُضاف بروفايل غائب)
- [x] بروفايل مخصص (`php artisan serve`) يشتغل ويُحفظ في profiles.json

> **ملاحظات التنفيذ:** أُنجِز في `Tools/TerminalLauncher/`. البناء 0/0. `ShellDef` وُسِّع بحقلَي WorkingDirectory/EnvironmentVariables مع بقاء API القديم (`All`/`Get`) متوافقاً.

---

## T-102: Tabs
**الحالة:** ⬜ لم يبدأ | **الأولوية:** P0 | **التقدير:** 4 أيام | **يعتمد على:** T-101

### Subtasks
- [ ] T-102.1: `TerminalSessionViewModel` يغلف (PtySession + Buffer + Parser) — كل تاب يملك واحد
- [ ] T-102.2: TabControl مخصص: إضافة (Ctrl+T)، إغلاق (Ctrl+W مع تأكيد إذا العملية شغالة)، تنقّل (Ctrl+Tab / Ctrl+1-9)
- [ ] T-102.3: عنوان التاب الديناميكي من OSC 0/2 + fallback لاسم البروفايل
- [ ] T-102.4: سحب وإفلات لإعادة ترتيب التابات
- [ ] T-102.5: مؤشر نشاط: التاب غير النشط يتميّز عند وصول output جديد
- [ ] T-102.6: زر + مع سهم يفتح قائمة البروفايلات

### معايير القبول
- [ ] 10 تابات بنفس الوقت، كل واحد بجلسة مستقلة، بدون تداخل بالـ output
- [ ] إغلاق تاب يحرر موارده (العملية تموت)

---

## T-103: Split Panes
**الحالة:** ⬜ لم يبدأ | **الأولوية:** P0 | **التقدير:** 5 أيام | **يعتمد على:** T-102

### Subtasks
- [ ] T-103.1: بنية شجرية `PaneNode` (Leaf = تيرمنال، Branch = تقسيم أفقي/عمودي بنِسَب)
- [ ] T-103.2: أوامر التقسيم: Alt+Shift+D (تلقائي بالاتجاه الأكبر)، أفقي/عمودي من الـ palette لاحقاً
- [ ] T-103.3: تنقّل بين الـ panes بـ Alt+Arrows + إبراز الـ pane النشط بإطار
- [ ] T-103.4: تغيير الحجم: سحب الفاصل بالماوس + Alt+Shift+Arrows
- [ ] T-103.5: إغلاق pane (بموت العملية أو Ctrl+W) يدمج المساحة بالشقيق
- [ ] T-103.6: حفظ بنية التقسيم ضمن بيانات التاب (تمهيداً لـ T-109)

### معايير القبول
- [ ] تقسيم 4 panes بتاب واحد، كل واحد بـ shell مختلف، الكل شغال
- [ ] Resize النافذة يوزّع النسب صح على الكل

---

## T-104: نسخ ولصق متقدم + Paste Protection
**الحالة:** ✅ مكتمل | **الأولوية:** P0 | **التقدير:** 3 أيام | **يعتمد على:** T-004

### Subtasks
- [x] T-104.1: تحديد بالماوس: سطري (click+drag موجود)، كلمة (double-click → `SelectWordAt`)، سطر (triple-click → `SelectLineAt`)
- [~] T-104.2: تحديد مستطيل (Alt+drag) — **مؤجَّل** (اختياري بالتاسك)
- [x] T-104.3: نسخ Ctrl+Shift+C (أو التحديد ثمّ الكتلة)؛ Ctrl+C ينسخ عند وجود تحديد وإلّا 0x03
- [x] T-104.4: لصق Ctrl+V + Bracketed Paste عند الوضع 2004 (`_coreScreen.BracketedPaste`)
- [x] T-104.5: **نافذة تحذير** (`PasteConfirmDialog`) عند نصّ متعدّد الأسطر أو أنماط خطرة (`rm -rf`/`del /s`/`format`/`Remove-Item -Recurse`/`dd if=`) — تعرض النصّ كاملاً قبل الموافقة
- [x] T-104.6: النسخ يقصّ الـ trailing whitespace (كان موجوداً في `GetSelectedText`)

### معايير القبول
- [x] لصق سكربت متعدّد الأسطر يعرض التحذير والموافقة تلصقه صح
- [x] النسخ من output ملوّن يعطي نصّاً نظيفاً

> **ملاحظات التنفيذ:** المنطق في `Controls/TerminalTabView.xaml.cs` + نافذة `Controls/PasteConfirmDialog.xaml(.cs)` + 7 مفاتيح `paste.*` (en+ar). كل مسارات اللصق (Ctrl+V/الزرّ الأيمن) تمرّ عبر `PasteFromClipboard()`. البناء 0/0.

---

## T-105: البحث داخل الـ Output
**الحالة:** ⬜ لم يبدأ | **الأولوية:** P0 | **التقدير:** 3 أيام | **يعتمد على:** T-004

### Subtasks
- [ ] T-105.1: شريط بحث (Ctrl+F) يطفو أعلى التيرمنال
- [ ] T-105.2: بحث بالـ Buffer كامل (شاشة + scrollback) مع highlight لكل النتائج وعدّاد (3/17)
- [ ] T-105.3: تنقّل Enter / Shift+Enter مع قفز الـ scroll للنتيجة
- [ ] T-105.4: خيارات: Case sensitive, Regex, كلمة كاملة
- [ ] T-105.5: البحث يتحدث مباشرة مع وصول output جديد

### معايير القبول
- [ ] بحث بـ 10,000 سطر scrollback يرجع نتائج فورياً (< 100ms)

---

## T-106: History محلي
**الحالة:** ⬜ لم يبدأ | **الأولوية:** P1 | **التقدير:** 3 أيام | **يعتمد على:** —

### Subtasks
- [ ] T-106.1: قاعدة SQLite (`%AppData%\<AppName>\data.db`) بجدول History: Command, ProfileId, WorkingDir, Timestamp, ExitCode(nullable)
- [ ] T-106.2: التقاط الأوامر: عبر Shell Integration (OSC 133) إن وُجد، وإلا استنتاج من سطر الإدخال عند Enter
- [ ] T-106.3: API استعلام: بحث نصي + fuzzy، فلترة بالبروفايل والمجلد
- [ ] T-106.4: حد أقصى قابل للتخصيص (افتراضي 50,000 أمر) + خيار مسح الـ history
- [ ] T-106.5: استثناء الأوامر الحساسة من التخزين (أنماط تحتوي password/token/secret)

### معايير القبول
- [ ] الأوامر تنخزن من جلستين مختلفتين وتظهر بالبحث

---

## T-107: الثيمات والخطوط
**الحالة:** ⬜ لم يبدأ | **الأولوية:** P1 | **التقدير:** 4 أيام | **يعتمد على:** T-005

### Subtasks
- [ ] T-107.1: موديل `ColorScheme` (16 لون + fg/bg/cursor/selection) + 4 ثيمات مدمجة (Dark/Light/One Dark/Dracula)
- [ ] T-107.2: استيراد ثيمات بصيغة Windows Terminal JSON و iTerm2 `.itermcolors`
- [ ] T-107.3: إعدادات الخط: العائلة (فلترة Monospace فقط)، الحجم، Weight + Zoom بـ Ctrl+Scroll و Ctrl+= / Ctrl+-
- [ ] T-107.4: شفافية الخلفية + Acrylic Blur (DWM APIs) كخيار
- [ ] T-107.5: ثيم مختلف لكل بروفايل (اختياري) + اتباع Dark/Light مال النظام

### معايير القبول
- [ ] استيراد ثيم iTerm2 حقيقي من الإنترنت يشتغل مباشرة
- [ ] تغيير الثيم يطبّق فوري على كل الجلسات المفتوحة

---

## T-108: Configuration as File + Hot Reload
**الحالة:** ⬜ لم يبدأ | **الأولوية:** P1 | **التقدير:** 3 أيام | **يعتمد على:** —

### Subtasks
- [ ] T-108.1: `SettingsService` مركزي: تحميل/حفظ `settings.json` مع JSON Schema للتوثيق والتحقق
- [ ] T-108.2: FileSystemWatcher: تعديل يدوي بالملف يطبّق فوري (مع debounce وتجاهل كتاباتنا الذاتية)
- [ ] T-108.3: إعدادات تالفة → رسالة واضحة + العمل بالافتراضي بدون crash + نسخة backup تلقائية
- [ ] T-108.4: نافذة Settings رسومية (أقسام: عام، مظهر، بروفايلات، اختصارات) تكتب على نفس الملف
- [ ] T-108.5: أمر "Open settings.json" يفتح الملف بالمحرر الافتراضي

### معايير القبول
- [ ] تعديل حجم الخط يدوياً بالملف يتطبق خلال ثانية بدون إعادة تشغيل

---

## T-109: Sessions (حفظ واستعادة)
**الحالة:** ⬜ لم يبدأ | **الأولوية:** P1 | **التقدير:** 3 أيام | **يعتمد على:** T-102, T-103

### Subtasks
- [ ] T-109.1: Serialize حالة النافذة: التابات، شجرة الـ splits، البروفايل، WorkingDirectory الحالي لكل جلسة (عبر OSC 9;9 أو استعلام العملية)، العناوين
- [ ] T-109.2: حفظ تلقائي دوري + عند الإغلاق → `session.json`
- [ ] T-109.3: استعادة عند التشغيل (خيار: دائماً / اسأل / أبداً)
- [ ] T-109.4: أمر "Restart Session" يعيد تشغيل جلسة ميتة بنفس البروفايل والمجلد
- [ ] T-109.5: جلسات مسماة: حفظ تخطيط الحالي باسم واستعادته لاحقاً

### معايير القبول
- [ ] إغلاق البرنامج بـ 3 تابات وتقسيمات ثم فتحه يرجّع نفس التخطيط والمجلدات

---

## T-110: Clickable Links & Paths
**الحالة:** ✅ مكتمل | **الأولوية:** P1 | **التقدير:** 2 أيام | **يعتمد على:** T-004

### Subtasks
- [x] T-110.1: كشف URLs بالـ Regex على **السطر المنقور** فقط (`Services/LinkDetector.cs`)
- [x] T-110.2: مسارات ويندوز (`C:\...`, `.\relative`) + يونكسيّة + مسارات بأرقام أسطر (`file.php:42`)
- [x] T-110.3: Ctrl+Click: URL→المتصفح، مجلد→Explorer، ملف→المحرّر، `file:line`→`code -g` (يرتدّ للمحرّر) — `Services/LinkOpener.cs`
- [~] T-110.4: مؤشّر اليد عند Ctrl+Hover ✅؛ **تسطير المكتشَف بالـRegex عند hover مؤجَّل** (تسطير OSC 8 موجود بالرندرر)
- [x] T-110.5: أولويّة OSC 8 hyperlink الصريح على الكشف

### معايير القبول
- [x] رابط من output (composer) ينفتح بالمتصفح
- [x] مسار خطأ Laravel (`app/Http/...php:25`) ينفتح على السطر (`code -g file:25`)

> **ملاحظات التنفيذ:** كشف نقيّ في `Services/LinkDetector.cs` (مُتحقَّق يدوياً على 6 حالات) + فتح آمن في `Services/LinkOpener.cs` (`Process.Start(UseShellExecute=true)` بعد التحقّق من الوجود). الربط في معالجات ماوس `TerminalTabView`. البناء 0/0. **اختبارات وحدة لم تُضَف** (لا مشروع اختبار WPF بعد).

---

# ✅ اختبار إغلاق المرحلة (Phase 1 Exit Test)
1. افتح 3 تابات (PowerShell, CMD, WSL) وقسّم واحد لـ pane-ين
2. سوّي شغل حقيقي: git, composer, php artisan serve
3. سكّر البرنامج وافتحه → كل شي رجع بمكانه
4. انسخ والصق سكربت متعدد الأسطر → التحذير يظهر
5. ابحث عن كلمة بالـ output → تنلقى
6. اضغط Ctrl+Click على مسار خطأ → ينفتح بالمحرر
7. **استخدمه أسبوع كامل بشغلك اليومي بدل Windows Terminal**
