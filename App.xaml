<Application x:Class="TerminalLauncher.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             StartupUri="MainWindow.xaml">
    <Application.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="Styles/Theme.xaml"/>
            </ResourceDictionary.MergedDictionaries>

            <!-- الخطّ: لاتيني Anthropic Sans، عربي Tajawal، ثم Segoe UI احتياطاً (نفس مشروع WPF) -->
            <FontFamily x:Key="Font.Ui">pack://application:,,,/Assets/Fonts/#Anthropic Sans, pack://application:,,,/Assets/Fonts/#Tajawal, Segoe UI, Tahoma</FontFamily>

            <!-- فراشٍ ابتدائية (دارك محايد دافئ) — يستبدلها ThemeManager عند الإقلاع -->
            <SolidColorBrush x:Key="Brush.Bg"          Color="#1A1A1A"/>
            <SolidColorBrush x:Key="Brush.Surface"     Color="#292826"/>
            <SolidColorBrush x:Key="Brush.Surface2"    Color="#333230"/>
            <SolidColorBrush x:Key="Brush.SurfaceHover" Color="#3D3B38"/>
            <SolidColorBrush x:Key="Brush.Border"      Color="#3A3936"/>
            <SolidColorBrush x:Key="Brush.Text"        Color="#ECEAE6"/>
            <SolidColorBrush x:Key="Brush.TextMuted"   Color="#9D998F"/>
            <SolidColorBrush x:Key="Brush.Accent"      Color="#C96442"/>
            <SolidColorBrush x:Key="Brush.AccentHover" Color="#D67A5A"/>
            <SolidColorBrush x:Key="Brush.AccentSoft"  Color="#C96442" Opacity="0.18"/>
            <SolidColorBrush x:Key="Brush.OnAccent"    Color="#FFFFFF"/>
            <SolidColorBrush x:Key="Brush.TerminalBg"  Color="#1A1A1A"/>
            <SolidColorBrush x:Key="Brush.HeaderScrim" Color="#292826" Opacity="0.78"/>
            <SolidColorBrush x:Key="Brush.SurfaceGlass" Color="#292826" Opacity="0.92"/>
            <SolidColorBrush x:Key="Brush.Danger"      Color="#E0603F"/>
            <SolidColorBrush x:Key="Brush.Success"     Color="#9ECE6A"/>

            <!-- شريط تمرير نحيل متوافق مع الثيم (مقبض دائريّ يتلوّن باللكنة عند المرور، مسار شفّاف، بلا أسهم).
                 مكتفٍ بذاته — يُطبَّق عبر ScrollViewer.Resources حيث يُراد (مثل لوحة الضبط). -->
            <Style x:Key="ThemedScrollBar" TargetType="ScrollBar">
                <Setter Property="Width" Value="9"/>
                <Setter Property="MinWidth" Value="9"/>
                <Setter Property="Background" Value="Transparent"/>
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="ScrollBar">
                            <Grid Background="Transparent">
                                <Track x:Name="PART_Track" IsDirectionReversed="True">
                                    <Track.DecreaseRepeatButton>
                                        <RepeatButton Command="ScrollBar.PageUpCommand" Focusable="False" Background="Transparent" BorderThickness="0">
                                            <RepeatButton.Template><ControlTemplate TargetType="RepeatButton"><Border Background="Transparent"/></ControlTemplate></RepeatButton.Template>
                                        </RepeatButton>
                                    </Track.DecreaseRepeatButton>
                                    <Track.IncreaseRepeatButton>
                                        <RepeatButton Command="ScrollBar.PageDownCommand" Focusable="False" Background="Transparent" BorderThickness="0">
                                            <RepeatButton.Template><ControlTemplate TargetType="RepeatButton"><Border Background="Transparent"/></ControlTemplate></RepeatButton.Template>
                                        </RepeatButton>
                                    </Track.IncreaseRepeatButton>
                                    <Track.Thumb>
                                        <Thumb>
                                            <Thumb.Template>
                                                <ControlTemplate TargetType="Thumb">
                                                    <Border x:Name="Th" CornerRadius="4" Margin="2,0" Background="{DynamicResource Brush.Border}"/>
                                                    <ControlTemplate.Triggers>
                                                        <Trigger Property="IsMouseOver" Value="True">
                                                            <Setter TargetName="Th" Property="Background" Value="{DynamicResource Brush.Accent}"/>
                                                        </Trigger>
                                                    </ControlTemplate.Triggers>
                                                </ControlTemplate>
                                            </Thumb.Template>
                                        </Thumb>
                                    </Track.Thumb>
                                </Track>
                            </Grid>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>
        </ResourceDictionary>
    </Application.Resources>
</Application>
